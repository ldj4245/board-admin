<?xml version="1.0"?>
<thlogic>

  <attr sel=".modal-body/pre" th:style="'white-space: pre-line'"/>

<!--  th:action 의 역할은 해당 form에 csrf 토큰을 자동으로 세팅하는데 사용됨. -->
  <attr sel=".modal-footer/form" th:action="@{#}" th:method="post" />
</thlogic>